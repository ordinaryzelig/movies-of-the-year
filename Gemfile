source 'http://rubygems.org'

gem 'activerecord', '3.1.3'
gem 'activesupport', '3.1.3' # Just for tmdb_party.
gem 'awesome_print'
gem 'ics', '0.3'
gem 'sqlite3', '1.3.5'
gem 'tmdb_party', '0.9.0', require: nil # Require manually because it depends on activesupport.

group :test do
  gem 'minitest', '2.10.0'
end
